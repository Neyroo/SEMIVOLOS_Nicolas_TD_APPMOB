<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.semivolos_nicolas_liste_de_sport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_sport"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SEMIVOLOS_Nicolas_liste_De_Sport">

        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Natation"/>
        <activity android:name=".BmxRace" />
        <activity android:name=".Handball" />
        <activity android:name=".Football" />
        <activity android:name=".Basketball" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            >
        </activity>
    </application>

</manifest>